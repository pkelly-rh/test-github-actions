name: Check for Console.logs()
# on: [push]
on: [pull_request1]
# [pull_request]:
#     types: [open]
#     branches:
#       - main
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      # - name: Check out repository code
      #   uses: actions/checkout@v2
      #   with:
      #     ref: ${{ github.event.pull_request.base.sha }}
      - run: echo nice "${{ github.event.pull_request.base.sha }}""

      # - name: Get source branch name
      #   shell: bash
      #   run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
      #   id: source_branch_name

      # - name: Get base sha
      #   shell: bash
      #   run: echo "##[set-output name=sha;]$(echo fooo ${github.event.pull_request.base.sha})"
      #   id: get_base_sha

      # # no need to specify `fetch-depth`, let it default to most recent commit only.
      # - run: echo "🔎 The name of your branch is ${GITHUB_REF#refs/heads/} and your repository is ${{ github.repository }}."
      # # - run: echo ${{ github.event.pull_request.base.sha }}
      # # - run: echo ${{ github.sha }}
      # - run: echo "SHA is ${{ steps.get_base_sha.outputs.sha }}" 
      # - run: echo "SHA should be ${{ github.event.pull_request.base.sha }}" 
      
      # - name: Fetch and Diff PR with base from which it was cloned
      #   # git fetch will fail if ${{ github.event.pull_request.base.sha }} is not set.
      #   # ${{ github.event.pull_request.base.sha }} WILL be set for event `pull_request`
      #   # if: ${{ steps.get_base_sha.outputs.sha }} 
      #   # run: |
      #   #   git fetch origin main ${{ steps.get_base_sha.outputs.sha }}
      #   #   echo git diff ${{ steps.get_base_sha.outputs.sha }} ${{ steps.source_branch_name.outputs.branch }} | grep console.log | wc -l
      #   run: |
      #     git fetch origin main ${{ steps.get_base_sha.outputs.sha }}
      #     echo git diff ${{ steps.get_base_sha.outputs.sha }} ${{ steps.source_branch_name.outputs.branch }} | grep console.log | wc -l
      #   id: total_console_logs

        # echo git diff ${{ github.event.pull_request.base.sha }} ${{ github.sha }} | grep console.log | wc -l